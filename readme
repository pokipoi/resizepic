本程序用来将奇数的图片像素拉伸为偶数，不是裁剪
使用：resizepic.exe放入需要更改像素的图片的文件夹内双击执行，生成后的文件会保存在当前目录的output文件下。

额外
resizepic.exe由ROOT_folder.py编译
main.py是另一个变体，输入文件夹是当前程序所在的文件夹下的input文件夹，输出的文件夹是当前程序所在的文件夹下的output文件夹。
test下是三张测试用图片

love by HPP